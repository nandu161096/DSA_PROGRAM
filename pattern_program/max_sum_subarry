def max_sum_subarry(arr,k):
    wind_sum = sum(arr[:k])
    max_sum = wind_sum
    
    for i in range(k, len(arr)):
        wind_sum += arr[i]
        wind_sum -= arr[i-k]
        max_sum = max(wind_sum,max_sum)
        
    return max_sum
    
arr = [2,1,5,1,3,2]
print(max_sum_subarry(arr,3))
