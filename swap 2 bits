#include <stdio.h>

int swap_bits(int n,int i,int j) {
    int bi = (n >> i) & 1;
    int bj = (n >> j) & 1;
    
    printf("%d %d \n",bi,bj);
    if (bi != bj) {
        n ^= (1 << i) | (1 << j);
    }
    return n;
}

int main() {
    printf("%u\n", swap_bits(28, 0, 3)); // swap bit 0 and 3
}
