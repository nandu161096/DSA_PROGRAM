#include <stdio.h>


int add(int a, int b) {
    return a+b;
}
int mul(int a, int b) {
    return a*b;
}
int main(void) {
    int (*fp[2])(int,int);
fp[0] = add;
fp[1] = mul;
    int b = (*fp[0])(3,5);
    printf("b %d\n",b);
    b = (*fp[1])(3,5);
    printf("b %d\n",b);
    return 0;
}
