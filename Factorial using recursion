#include <stdio.h>

int fact (int n) {
    if (n == 0) {
        return 1;
    }
    return n * fact(n-1);
}

int main()
{
    int facto = fact(5);
    printf("Hello World %d",facto);
    return 0;
}
